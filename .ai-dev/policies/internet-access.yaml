schema_version: "1.0"
external_content_is_untrusted: true
never_send: [secrets, production_like_data, credentials]
agents:
  conversation: allowlist
  developer: allowlist
  system-reviewer: allowlist
  business-reviewer: unrestricted_read
  qa: allowlist
global_denials: [download_execution, form_submission, automatic_login, billing_contract]
